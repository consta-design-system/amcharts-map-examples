import { createStand } from '##/stand/standConfig';

export default createStand({
  title: 'Заливка',
  id: 'patterns',
  group: 'examples',
  alias: ['фон', 'заливка', 'заливки', 'карта'],

  order: 30,
});
