import { createStand } from '##/stand/standConfig';

export default createStand({
  title: 'Группы точек',
  id: 'groups',
  group: 'examples',
  alias: ['точка', 'точки', 'группа', 'группы', 'карта'],
  order: 25,
});
